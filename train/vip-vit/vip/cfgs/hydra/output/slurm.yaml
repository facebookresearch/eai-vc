# @package _global_
hydra:
  run:
    dir: /checkpoint/yixinlin/eaif/results/vip_training/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.num}_${hydra.job.override_dirname}
  sweep:
    dir: /checkpoint/yixinlin/eaif/results/vip_training/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.num}_${hydra.job.override_dirname}

# # @package _global_
# hydra:
#   run:
#     dir: /checkpoint/jasonyma/GoFAR/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
#     subdir: ${hydra.job.num}_${hydra.job.override_dirname}
#   sweep:
#     dir: /checkpoint/jasonyma/GoFAR/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
#     subdir: ${hydra.job.num}_${hydra.job.override_dirname}

